rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any user to read shifts, but only authenticated users to write/delete.
    match /shifts/{shiftId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
